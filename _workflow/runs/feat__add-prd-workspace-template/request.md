# Active Request

Add the public `setup-prd-workspace` project to the DevKofi templates catalog.

Source request: `@GitHub add this to devkofi templates`

Confirmed decision: show a primary **Use Template** action that opens `https://github.com/kofiarhin/setup-prd-workspace`.
