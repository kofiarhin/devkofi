# Routines

No routine is active by default.

Any future routine must define its trigger, inputs, output, permissions, limits, failure behavior, and human review. Activation requires a separate explicit request and approval.
